apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-service
  labels:
    app: event-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: event-service
  template:
    metadata:
      labels:
        app: event-service
    spec:
      containers:
      - name: event-service
        image: event-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: team3-config
        - secretRef:
            name: team3-secrets
        env:
        - name: DATABASE_URL
          valueFrom:
            configMapKeyRef:
              name: team3-config
              key: DATABASE_URL_EVENT
        - name: ALGORITHM
          value: "HS256"
---
apiVersion: v1
kind: Service
metadata:
  name: event-service
spec:
  selector:
    app: event-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
