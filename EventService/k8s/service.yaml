apiVersion: v1
kind: Service
metadata:
  name: event-service
  labels:
    app: event-service
spec:
  type: ClusterIP
  selector:
    app: event-service
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
